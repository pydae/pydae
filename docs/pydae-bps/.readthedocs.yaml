# ReadTheDocs build configuration for pydae-bps.
#
# To publish: create a ReadTheDocs project whose "Configuration file path"
# points to  docs/pydae-bps/.readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/pydae-bps/conf.py
  fail_on_warning: false

python:
  install:
    - requirements: docs/pydae-bps/requirements.txt
    - method: pip
      path: packages/pydae-core
    - method: pip
      path: packages/pydae-bps

formats:
  - htmlzip
  - pdf
